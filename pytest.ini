[pytest]
addopts = -v --tb=short
testpaths = tests
pythonpath = .
markers =
    e2e: end-to-end Playwright tests
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %H:%M:%S
