# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# Auth session — contains real session tokens, never commit
auth/auth.json

# Browser profile — contains cookies/session data
.browser-profile/

# Playwright / pytest output
test-results/
.pytest_cache/
traces/

# Allure reporting
allure-results/
allure-report/

# Node (leftover from prior TypeScript setup)
node_modules/

# Environment variables (local secrets — never commit)
.env
